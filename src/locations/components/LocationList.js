import React from 'react';

import './LocationList.css';

const LocationList = props => {

    if (props.items.length === 0){
        return <div className="location-list center"></div>
    }

};

export default LocationList;